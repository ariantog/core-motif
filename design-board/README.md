# Corenation Design Studio (board)

Internal preview for all lines.

```bash
cd design-board
npm install
npm run dev
```

**http://localhost:5177**

Assets are served from `public/assets` → `/workspace/assets`.
