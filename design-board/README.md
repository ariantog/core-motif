# Corenation Design Studio (board)

Internal preview for all lines.

```bash
cd design-board
npm install
npm run dev
```

**http://localhost:5177**

Tabs: Brand · Men kanji · Women floral · Culture run · **Pace** · **Padel** · Marks.

Assets are served from `public/assets` → `/workspace/assets` (see `assets/README.md` for the folder map).
